// const joi = require('joi')

// exports.officeValidation = (data) => {
     
//     const Office = Joi.object({
//         level: Joi.string().min(1).max(1000).required(),
//         bcID: Joi.string().min(1).max(255).required(),
//         admin: Joi.string().min(4).max(255).required().email(),
//         phone: Joi.string().min(4).max(20).required().pattern(/^\+|\d[\s\d\-\(\)]*\d$/),

//     })
//     return Office.validate(data)
// }
